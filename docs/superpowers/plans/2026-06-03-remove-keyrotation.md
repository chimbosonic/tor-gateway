# Remove `TorServicePolicy.keyRotation` Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Delete the unimplemented `KeyRotationSpec` type and the `TorServicePolicySpec.KeyRotation` field from `policy.torgateway.io/v1alpha1`, regenerate the derived artefacts (deepcopy + CRD YAML + chart-synced CRD YAML), and confirm the operator still builds, tests green, and lints clean.

**Architecture:** This is a pure deletion of dead API surface — no controller logic, tests, or e2e fixtures reference `keyRotation` today (verified by `grep` in the brainstorm). The Go type at `api/v1alpha1/torservicepolicy_types.go` is the source of truth; `zz_generated.deepcopy.go` is regenerated by `make generate`, the CRD YAML under `config/crd/bases/` by `make manifests`, and the chart copy under `charts/tor-gateway/files/crds/` by `make chart-sync`. Pre-flight verification (baseline green) makes any post-change failure unambiguously attributable to the deletion.

**Tech Stack:** Go + kubebuilder (controller-runtime), `controller-gen` for codegen, the repo's `Makefile` (`generate`, `manifests`, `test`, `lint`, `chart-sync`).

**Spec:** [`docs/superpowers/specs/2026-06-03-remove-keyrotation-design.md`](../specs/2026-06-03-remove-keyrotation-design.md).

---

## File Structure

Files this plan touches:

| Path | Role | How |
|---|---|---|
| `api/v1alpha1/torservicepolicy_types.go` | Source of truth — Go API types | Manual edit (Task 2) |
| `api/v1alpha1/zz_generated.deepcopy.go` | Generated DeepCopy helpers | `make generate` (Task 3) |
| `config/crd/bases/policy.torgateway.io_torservicepolicies.yaml` | Generated CRD YAML (source-of-truth copy) | `make manifests` (Task 3) |
| `charts/tor-gateway/files/crds/policy.torgateway.io_torservicepolicies.yaml` | Chart copy of the CRD | `make chart-sync` (Task 3) |
| `~/.claude/projects/-Volumes-source-code-Personal-torGateway/memory/project_torgateway_followups.md` | Auto-memory follow-ups index entry | Manual edit (Task 6, not committed) |

No tests are created or modified — no existing test references `keyRotation` (the spec records the grep evidence). The existing chart-sync drift check in CI will catch any forgotten regeneration step.

---

## Task 1: Pre-flight — confirm baseline green

**Files:** none (verification only).

The point of this task is to fail loudly *before* any change if the working tree is already broken, so failures in Task 4 are unambiguously attributable to the deletion.

- [ ] **Step 1: Confirm clean working tree**

Run: `git status --short`
Expected: empty output (no uncommitted changes).

If the tree is not clean, stop and ask the user how to proceed. Do not stash, do not discard.

- [ ] **Step 2: Run the full test suite**

Run: `make test`
Expected: PASS. (Target chain: `manifests generate fmt vet setup-envtest` then runs `go test`.)

- [ ] **Step 3: Run the linter**

Run: `make lint`
Expected: PASS. (Runs `golangci-lint`.)

- [ ] **Step 4: Confirm baseline diff is empty after codegen**

Codegen ran in Step 2 as part of `make test`. Confirm it produced no diff.

Run: `git status --short`
Expected: empty output.

If non-empty, the tree contained stale generated files before this plan started. Stop and ask the user — committing stale-codegen artefacts inside this plan's commit would muddy the diff.

---

## Task 2: Delete the Go type and field

**Files:**
- Modify: `api/v1alpha1/torservicepolicy_types.go` (lines 29–46 and 101–106 at HEAD)

This is the only manual edit. Everything else regenerates from here.

- [ ] **Step 1: Remove the `KeyRotationSpec` type block**

In `api/v1alpha1/torservicepolicy_types.go`, delete the entire block (lines 29–46 at HEAD inclusive of the leading comment), which currently reads:

```go
// KeyRotationSpec controls automatic rotation of a Gateway's hidden-service
// ed25519 key. Rotation generates a new .onion address (the address IS the
// key); it is therefore opt-in and disabled by default.
type KeyRotationSpec struct {
	// Schedule (cron format, UTC) at which the key is rotated. When unset,
	// rotation is disabled even if this struct is present.
	// +optional
	Schedule string `json:"schedule,omitempty"`

	// RetainPreviousKeys controls how many previous keys the operator keeps
	// in the Secret after rotation (so external systems can still resolve
	// stale .onion addresses for a grace period). Defaults to 0.
	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=10
	// +kubebuilder:default=0
	// +optional
	RetainPreviousKeys int32 `json:"retainPreviousKeys,omitempty"`
}
```

The preceding `// TorLogLevel ...` block (lines 25–27) and the following `// TorServicePolicySpec ...` block must remain. Leave exactly one blank line between them after the deletion.

- [ ] **Step 2: Remove the `KeyRotation` field from `TorServicePolicySpec`**

In the same file, delete the field block (lines 101–106 at HEAD), which currently reads:

```go
	// KeyRotation configures automatic ed25519 key rotation. Enabling
	// rotation will change the published .onion address; clients with
	// bookmarks will need to update.
	//
	// +optional
	KeyRotation *KeyRotationSpec `json:"keyRotation,omitempty"`
```

Including the blank line immediately above it (so the previous field — `Resources *corev1.ResourceRequirements` — ends with its own closing brace immediately before the struct's closing `}`).

- [ ] **Step 3: Verify the file compiles (no other references exist)**

Run: `go build ./api/v1alpha1/...`
Expected: success, no error output.

If `go build` reports `undefined: KeyRotationSpec` from anywhere, stop — the brainstorm grep missed a reference and the plan needs to absorb that file before proceeding.

- [ ] **Step 4: Confirm no stale references survive in `api/v1alpha1/`**

Run: `grep -n 'KeyRotation\|keyRotation\|RetainPreviousKeys\|retainPreviousKeys' api/v1alpha1/*.go`
Expected: only matches in `zz_generated.deepcopy.go` (which Task 3 regenerates). Specifically expect three lines that mention `KeyRotationSpec` (the DeepCopyInto, the DeepCopy header, and the `new(KeyRotationSpec)` call) plus the `in.KeyRotation != nil` conditional. No matches in `torservicepolicy_types.go`.

If any other `.go` file in this directory still mentions the symbol, you missed an edit. Re-read Task 2 steps 1–2.

---

## Task 3: Regenerate derived artefacts

**Files (all regenerated, do not hand-edit):**
- Modify: `api/v1alpha1/zz_generated.deepcopy.go`
- Modify: `config/crd/bases/policy.torgateway.io_torservicepolicies.yaml`
- Modify: `charts/tor-gateway/files/crds/policy.torgateway.io_torservicepolicies.yaml`

- [ ] **Step 1: Regenerate the DeepCopy code**

Run: `make generate`
Expected: success. The diff to `api/v1alpha1/zz_generated.deepcopy.go` should remove:
- The `DeepCopyInto`/`DeepCopy` pair for `KeyRotationSpec` (currently lines 44–57 at HEAD).
- The conditional `if in.KeyRotation != nil { ... }` block inside `TorServicePolicySpec.DeepCopyInto` (currently lines 366–370 at HEAD).

- [ ] **Step 2: Regenerate the CRD YAML**

Run: `make manifests`
Expected: success. The diff to `config/crd/bases/policy.torgateway.io_torservicepolicies.yaml` should remove the `keyRotation:` node and its children (currently lines 62–83 at HEAD).

- [ ] **Step 3: Sync the chart copy of the CRD**

Run: `make chart-sync`
Expected: success. The diff to `charts/tor-gateway/files/crds/policy.torgateway.io_torservicepolicies.yaml` should mirror Step 2 — same `keyRotation` block removed.

- [ ] **Step 4: Confirm the produced YAML has no surviving `keyRotation` node**

Run: `grep -n 'keyRotation\|retainPreviousKeys' config/crd/bases/policy.torgateway.io_torservicepolicies.yaml charts/tor-gateway/files/crds/policy.torgateway.io_torservicepolicies.yaml`
Expected: no output (exit status 1).

If either file still mentions the field, re-run the corresponding make target.

- [ ] **Step 5: Confirm the produced Go has no surviving `KeyRotation` symbol**

Run: `grep -n 'KeyRotation\|RetainPreviousKeys' api/v1alpha1/zz_generated.deepcopy.go`
Expected: no output (exit status 1).

If matches remain, run `make generate` again — `controller-gen` should be idempotent, so a surviving match means the source-of-truth file (`torservicepolicy_types.go`) still has the type. Re-check Task 2.

---

## Task 4: Verify the tree is still green

**Files:** none (verification only).

- [ ] **Step 1: Run the full test suite**

Run: `make test`
Expected: PASS. No tests should newly fail — `keyRotation` was never observed by any test (verified in the brainstorm grep).

If any test fails, do not "fix" the test to make it pass. Read the failure: a passing baseline (Task 1) followed by a failure here means either (a) a test references the deleted symbol in a way grep missed, or (b) the deletion accidentally removed something the brainstorm didn't account for. Either case requires going back to Task 2, not patching forward.

- [ ] **Step 2: Run the linter**

Run: `make lint`
Expected: PASS. Lint should not produce new findings — the deletion only removes content.

- [ ] **Step 3: Confirm no `keyRotation` references survive repo-wide (outside historical specs)**

Run:
```
grep -rn 'KeyRotation\|keyRotation\|RetainPreviousKeys\|retainPreviousKeys' \
    --include='*.go' --include='*.yaml' --include='*.md' . \
    | grep -v '^./vendor\|^./bin\|^./docs/superpowers/specs/'
```
Expected: no output (exit status 1).

The exclusion for `docs/superpowers/specs/` is intentional — three historical spec docs reference the field by name as part of past decision records. The spec for this deletion lives in the same directory and intentionally mentions the field as well. Those are immutable history; do not edit them.

If anything *else* shows up (any non-spec `.md`, any `.go`, any `.yaml` outside the regenerated CRDs), the deletion is incomplete. Read the file, fix forward, and re-run this step.

---

## Task 5: Commit

**Files:** the four modified files from Tasks 2 and 3, as a single commit.

- [ ] **Step 1: Stage exactly the four files**

Run:
```
git add api/v1alpha1/torservicepolicy_types.go \
        api/v1alpha1/zz_generated.deepcopy.go \
        config/crd/bases/policy.torgateway.io_torservicepolicies.yaml \
        charts/tor-gateway/files/crds/policy.torgateway.io_torservicepolicies.yaml
```

- [ ] **Step 2: Verify the staged set matches the plan**

Run: `git status --short`
Expected: exactly four `M ` lines for the four files above, and nothing in the unstaged ("right column") position.

If anything else is staged or anything is unstaged, stop and inspect — Task 3 may have produced an unexpected diff.

- [ ] **Step 3: Commit**

Run (the `--author` local-part is the current model ID per the global CLAUDE.md convention; `--no-gpg-sign` is required because the user signs via smartcard later):

```
git commit --no-gpg-sign \
  --author="Alexis Lowe <claude-opus-4-7@chimbosonic.com>" \
  -m "$(cat <<'EOF'
refactor(api): drop unimplemented TorServicePolicy.keyRotation

The keyRotation field and its KeyRotationSpec type had no controller
logic, and the RetainPreviousKeys/Schedule shape did not fit either
real rotation use case (pseudonymity churn or compromise repudiation).
Future rotation work will be designed from scratch alongside
onionbalance HA. See
docs/superpowers/specs/2026-06-03-remove-keyrotation-design.md.
EOF
)"
```

Expected: success. Do not add a `Co-Authored-By:` trailer. Do not run `git push` — the user pushes manually after `git rebase --signoff`.

- [ ] **Step 4: Confirm the commit is clean**

Run: `git status --short`
Expected: empty output.

Run: `git log -1 --format='%an <%ae>%n%s'`
Expected:
```
Alexis Lowe <claude-opus-4-7@chimbosonic.com>
refactor(api): drop unimplemented TorServicePolicy.keyRotation
```

If author or subject line don't match, do not amend silently — surface the discrepancy.

---

## Task 6: Update auto-memory follow-ups

**Files:**
- Modify: `~/.claude/projects/-Volumes-source-code-Personal-torGateway/memory/project_torgateway_followups.md`

Not committed to the repo — auto-memory lives under the user's home directory.

- [ ] **Step 1: Replace the "Still open (deferred)" `KeyRotationSpec` bullet**

In `~/.claude/projects/-Volumes-source-code-Personal-torGateway/memory/project_torgateway_followups.md`, find the section headed `**Still open (deferred):**` containing the `KeyRotationSpec is dead code` bullet. Replace the entire `**Still open (deferred):**` section with a new section headed `**Closed by deletion (2026-06-03):**` containing a single bullet of the form:

> `**KeyRotationSpec` removed.** `TorServicePolicy.KeyRotation` and its `Schedule`/`RetainPreviousKeys` fields were never wired to controller logic and their shape did not fit either real rotation use case. Resolved by deletion, not by deferral. See `docs/superpowers/specs/2026-06-03-remove-keyrotation-design.md` and commit `<short SHA from Task 5 Step 4>`.

Do not invent a release version (`v0.3.4`, `v0.4.0`, etc.) — release sequencing is the user's call. The bare date is sufficient.

The intent: a future session reading this memory should see that the dead-field follow-up was closed by *deletion*, not by deferral to the onionbalance brainstorm.

- [ ] **Step 2: Update the `description:` frontmatter line if needed**

If the file's frontmatter `description:` line still says `most closed by v0.3.1 / v0.3.2; remaining open: retainPreviousKeys dead field (deferred to onionbalance brainstorm)`, replace the trailing clause to reflect that no follow-ups remain open. Example new value: `all follow-ups closed (probes/events RBAC v0.3.1, NetworkPolicy v0.3.2, keyRotation removed 2026-06-03)`.

- [ ] **Step 3: Confirm there is nothing left to do**

The repo work is done; memory is updated; the user will handle `git rebase --signoff` and `git push`. Hand off back to the user with the next-action note from the global CLAUDE.md: "Local commit is in place — run `git rebase --signoff` then `git push` when ready."
